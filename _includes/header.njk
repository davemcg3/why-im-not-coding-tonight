  <header>
    <a href="{{ '/' | url }}" class="borderBlink">Why I'm Not Coding Tonight</a>
    <span><a href="{{ '/about' | url }}">About</a></span>
  </header>
  {%- if pagination.pages %}
    <nav>
      {%- for pageEntry in pagination.pages %}
        <a href="{{ pagination.hrefs[ loop.index0 ] }}"{% if page.url == pagination.hrefs[ loop.index0 ] %} aria-current="page"{% endif %}>{{ loop.index }} </a>
      {%- endfor %}
    </nav>
  {% endif %}
